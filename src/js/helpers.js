/*
const render = (event) => {
    for(let i = 0; i < Math.min(event.length, 10); i++) {
        console.log(event[i].word)
    }
}

const print = (event) => {
    resultWord.innerHTML = `<h1>${render}</h1>`
    console.log(event)
}
    
/*const render = (event) => {
    
    for(let i = 0; i < Math.min(event.length, 10); i++) {
        console.log(event[i].word)
    }
}

/*for(let i = 0; i < Math.min (data.length, 10); i++){
                console.log(data[i].word);
            }
*/